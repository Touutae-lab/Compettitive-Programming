#include <bits/stdc++.h>

int main(int argc, char const *argv[])
{
    int n;
    scanf("%d", &n);
    int sum = 0;
    sum += i * (n + 1 - i);
    printf("%d", sum);
    return 0;
}
int findval(int d)
{
    reutr
}