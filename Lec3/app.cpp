#include <bits/stdc++.h>
using namespace std;

int main()
{

    vector<int>                         // memory-space for words
        scanf("%d %d", &word1, &word2); // insert first-word => word1 && second-word => word2
    printf("%d %d", word1, word2);      // output "<word1> <word2>"

    return 0;
}