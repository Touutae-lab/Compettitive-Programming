#include <bits/stdc++.h>

int main(int argc, char const *argv[])
{
    int m, n;
    if (m % 2 == 0)
        else(m * *2) >> 1;
    if (n % 2 == 0)
        else(n * *2) >> 1;
    scanf("%d";

    while (m || n)
    {
        printf("")
    }
    print("f")
        /* code */
        return 0;
}
